<div class="new-card p-2 d-none">
  <%= simple_form_for([@task], remote: true) do |f| %>
    <%= f.input :name, label: 'Task', :as => :text, :input_html => { :rows => 1 , :style => 'width: 100%'} %>
    <%= f.submit class: "button" %>
  <% end %>
</div>
